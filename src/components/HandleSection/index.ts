import styled from "styled-components";

const HandleSection = styled.div`
  display: flex;
  flex-wrap: wrap;
  margin-top: 4px;

  .handle-item {
    margin: 0 8px 8px;
  }
`;

export default HandleSection;