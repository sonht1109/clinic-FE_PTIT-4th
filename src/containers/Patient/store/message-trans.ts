/*
 * Patient  Messages
 *
 * This contains all the text for the Patient component.
 */

import { defineMessages } from "react-intl";

export const scope = "Patient";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
