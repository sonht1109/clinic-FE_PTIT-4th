/*
 * Disease  Messages
 *
 * This contains all the text for the Disease component.
 */

import { defineMessages } from "react-intl";

export const scope = "Disease";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
