/*
 * Drug  Messages
 *
 * This contains all the text for the Drug component.
 */

import { defineMessages } from "react-intl";

export const scope = "Drug";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
