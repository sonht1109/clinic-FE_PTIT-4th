/*
 * Nurse  Messages
 *
 * This contains all the text for the Nurse component.
 */

import { defineMessages } from "react-intl";

export const scope = "Nurse";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
