/*
 * Doctor  Messages
 *
 * This contains all the text for the Doctor component.
 */

import { defineMessages } from "react-intl";

export const scope = "Doctor";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
