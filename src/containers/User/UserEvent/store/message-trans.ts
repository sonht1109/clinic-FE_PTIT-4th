/*
 * UserEvent  Messages
 *
 * This contains all the text for the UserEvent component.
 */

import { defineMessages } from "react-intl";

export const scope = "UserEvent";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
