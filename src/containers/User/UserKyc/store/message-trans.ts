/*
 * UserKyc  Messages
 *
 * This contains all the text for the UserKyc component.
 */

import { defineMessages } from "react-intl";

export const scope = "UserKyc";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
