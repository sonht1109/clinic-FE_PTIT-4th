/*
 * SystemSetup  Messages
 *
 * This contains all the text for the SystemSetup component.
 */

import { defineMessages } from "react-intl";

export const scope = "SystemSetup";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
