/*
 * UserAccountManagement  Messages
 *
 * This contains all the text for the UserAccountManagement component.
 */

import { defineMessages } from "react-intl";

export const scope = "UserAccountManagement";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
