/*
 * AdminRankReport  Messages
 *
 * This contains all the text for the AdminRankReport component.
 */

import { defineMessages } from "react-intl";

export const scope = "AdminRankReport";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
