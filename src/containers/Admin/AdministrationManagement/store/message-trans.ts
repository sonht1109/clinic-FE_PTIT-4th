/*
 * AdministrationManagement  Messages
 *
 * This contains all the text for the AdministrationManagement component.
 */

import { defineMessages } from "react-intl";

export const scope = "AdministrationManagement";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
