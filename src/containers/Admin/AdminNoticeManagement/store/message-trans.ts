/*
 * AdminNoticeManagement  Messages
 *
 * This contains all the text for the AdminNoticeManagement component.
 */

import { defineMessages } from "react-intl";

export const scope = "AdminNoticeManagement";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
